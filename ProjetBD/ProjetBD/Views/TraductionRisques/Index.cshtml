@model IEnumerable<ProjetBD.Models.TraductionRisque>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DesRisLgue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Langue.designation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Risque.libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DesRisLgue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Langue.designation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Risque.libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTraduction }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTraduction }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTraduction })
        </td>
    </tr>
}

</table>
