@model IEnumerable<ProjetBD.Models.Langue>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.designation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.designation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codeLangue }) |
            @Html.ActionLink("Details", "Details", new { id=item.codeLangue }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codeLangue })
        </td>
    </tr>
}

</table>
