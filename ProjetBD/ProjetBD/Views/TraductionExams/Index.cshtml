@model IEnumerable<ProjetBD.Models.TraductionExam>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DesExamLgue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Examan.periodicite)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Langue.designation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DesExamLgue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Examan.periodicite)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Langue.designation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTraduction }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTraduction }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTraduction })
        </td>
    </tr>
}

</table>
